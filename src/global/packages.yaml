# src/global/packages.yaml
# this filepath is for vibe coders

packages:
  python:
    interaction-server: "src.dev.backend.winapi.interaction-server"
    interaction-client: "src.dev.interaction-client"