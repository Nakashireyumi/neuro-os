relay_connection:
  host: 127.0.0.1
  port: 8765  # Intermediary port for monitoring
  backend_port: 8001  # Neuro-relay backend port (fake Neuro backend for integrations)
  auth_token: super-secret-token
integrations:
  spotify:
    enabled: true
  discord:
    enabled: true
  twitch:
    enabled: true
logging:
  level: INFO
  file: logs/neuro_os.log
